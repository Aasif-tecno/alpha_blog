<div id="home-container">
    <div class="jumbotron text-center">
        <h1 class="display-1 mt-5">Alpha Blog</h1>
        <p class="lead"> Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. augue in. Arcu bibendum at varius vel pharetra vel. </p>
        <hr class="my-4">
        <p>Ante metus dictum at tempor commodo ullamcorper. Lacus suspendisse faucibus sed. </p>
        <% if  !user_signed_in? %>
          <%= link_to "Sign Up",  new_user_registration_path, class:"btn btn-success btn-lg"%>
          <%= link_to "Log In", new_user_session_path, class:"btn btn-success btn-lg" %>
      
      <%else%>

       <%= link_to "Create Articles" , new_article_path, class:"btn btn-primary btn-lg"%>

    </div>
</div>

<%# <div id="page-content mt-4"> %>
<div class="container">
  <h1 class="font-weight-light mt-4 text-black">Articles</h1>
    <table class="table table-striped table-bordered table-dark table-hover col-sm-4 ">
      <thead>
        <tr>
          <th>User</th>
          <th>TITLE</th>
          <th>DESCRIPTION</th>
          <th>Created at</th>
          <th>Updated at</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% @articles.each do  |article|%>
        <%# <% if article.user == current_user %>              
        <tr>
          <td> <%= article.user.email.split("@")[0].capitalize %></td>
          <td><%= link_to article.title, article_path(article), class:"text-white"%></td>
          <td><%= truncate(article.description, length: 50) %></td> 
          <td><%= time_ago_in_words(article.created_at)%>  
          <td><%= time_ago_in_words(article.updated_at)%>  
          <td><%= link_to "Show", article_path(article), class:"btn btn-primary"%>  
        </tr>
        <% end %>
        <% end %> 
      </tbody>
    </table>
</div>
  </div> 

      
  
<br/><br/>
<br/><br/>


